
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lavnder Recovery</title>
    <style>
        h1{
            color:lavender;
            font-family: 'Redressed', cursive;
            text-align: center;
            font-size: 50px;
            margin-top: 100px;
        }
         .pro-photo {
            position: relative;
            width: 300px;
            height: 300px;
            overflow: hidden;
            border-radius: 50%;
            margin-left: 160px;
            
            
        }
        .pro-photo img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            
        }

        textarea {
            width: 600px;
            height: 150px;
            padding: 12px 20px;
            box-sizing: border-box;
            border: 2px solid lavender;
            border-radius: 4px;
            font-size: 16px;
            resize: none;
            background:linear-gradient(to top, rgba(0, 0, 0, 1), rgba(0,0,0,0));    
            margin-top: 50px;
            color: lavender;

            }
        .edit-all{
            margin-left: 600px;
            
        }
        .update{
            text-decoration: none;
            border:lavender;
            border-radius: 16px;
            border-style: none;
            color: lavender;
            background-color: black;
            padding: 4px 2px;
            border-style: solid;
        }
        body{
            background-image: url("https://i.imgur.com/iuy5TiA.jpg");
            background-size:cover;
        }
    </style>
</head>
<body>
    <h1>Re-tell Your Story</h1>
<div class="edit-all">
    <div class="pro-photo">
        <img src="<%= user.profileImage %>" alt="profile image">
    </div>

    <form action="/profile/?_method=PUT" method="POST" >
        <textarea name="bio"><%= user.bio %></textarea>
        <button class="update" type="submit">Update</button>

    </form>
</div>
</body>
</html>

